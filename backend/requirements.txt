# Runtime dependencies. Authoritative versions live in pyproject.toml;
# this file exists for `pip install -r`.

openpyxl>=3.1
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Development / test
pytest>=8.0
