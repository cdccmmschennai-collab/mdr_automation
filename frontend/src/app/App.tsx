/**
 * Application root.
 *
 * Routing goes here when there is more than one page. There is one page,
 * so there is no router yet.
 */

import { AutomatePage } from '../pages/AutomatePage';

export function App() {
  return <AutomatePage />;
}
